version: '3'

services:
  dckr:
    build: .
    env_file:
      - .env
    ports:
      - '${MESH_PORT}:${MESH_PORT}'
    volumes:
      - './app:/app'
    stdin_open: true
    tty: true
    command: "/usr/local/bin/python3.8 main.py"
